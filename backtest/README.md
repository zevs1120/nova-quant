# Backtest / Validation Layer

Status: `EXPERIMENTAL` (walk-forward + historical replay integrated; still not tick-level execution realism)

Contains out-of-sample validation utilities.

Entry module: `backtest/index.js`
