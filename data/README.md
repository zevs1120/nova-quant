# Data Layer

Status: mixed
- `MOCK_DATA`: sample fixtures and simulated artifacts
- `EXPERIMENTAL`: generated backtest/paper snapshots
- `REAL_PATH_READY`: folder layout prepared for real adapters

This folder intentionally separates data artifacts from research logic.
