# Standalone Node server
PORT=8787
SERVE_WEB_DIST=1

# Database
DB_PATH=/opt/nova-quant/data/quant.db
NOVA_DISABLE_SQLITE_PROCESS_LOCK=1

# Marvix runtime
NOVA_RUNTIME_MODE=cloud-openai-compatible
NOVA_CLOUD_OPENAI_BASE_URL=https://your-openai-compatible-endpoint/v1
NOVA_CLOUD_API_KEY=replace-me
NOVA_CORE_MODEL=Qwen/Qwen3-8B-Instruct
NOVA_SCOUT_MODEL=Qwen/Qwen3-4B-Instruct
NOVA_RETRIEVE_MODEL=BAAI/bge-m3
NOVA_CHALLENGER_MODEL=Qwen/Qwen3-14B-Instruct

# Optional hosted providers
GROQ_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=

# Market data ingestion
MASSIVE_API_KEY=replace-me

# Model ingest token (EC2 → API auth)
NOVA_MODEL_INGEST_TOKEN=replace-me

# Auth: Supabase Postgres (recommended)
# If NOVA_AUTH_DATABASE_URL is omitted, auth falls back to NOVA_DATA_DATABASE_URL.
NOVA_AUTH_DRIVER=postgres
NOVA_AUTH_DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
NOVA_AUTH_PG_SSL=require

# Business data: Supabase Postgres target
NOVA_DATA_DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres
NOVA_DATA_PG_SCHEMA=novaquant_data
# Optional canary: switch runtime reads to Postgres instead of SQLite-first mirror mode.
# Keep disabled until the production cutover checklist in docs/AWS_EC2_DEPLOYMENT.md is complete.
# NOVA_DATA_RUNTIME_DRIVER=postgres
# Optional hot-path protection: after a Postgres timeout, skip sync SQLite fallback and cool down retries.
# NOVA_PG_PRIMARY_READ_FAILURE_COOLDOWN_MS=60000
# NOVA_ALLOW_SYNC_HOT_PATH_FALLBACK=0

# Auth: Upstash Redis (legacy — use Postgres above instead)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Auth email delivery
# Resend custom logic has been removed. Configure SMTP delivery inside 
# your Supabase Auth Dashboard using an external provider like AWS SES or Resend SMTP.
NOVA_APP_URL=https://your-domain.com

# Admin access
NOVA_ADMIN_EMAILS=admin@novaquant.com
NOVA_OWNER_EMAIL=

# Public search expansion
ALPHA_VANTAGE_API_KEY=
COINGECKO_DEMO_API_KEY=
